<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true">

    <ScrollView
        android:id="@+id/vac_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/vac_layout_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Patient name -->
            <EditText
                android:id="@+id/vac_txt_patient_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/vac_label_patient_name"
                android:inputType="textPersonName" />

            <!-- Patient ID -->
            <EditText
                android:id="@+id/vac_txt_patient_id"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/vac_label_patient_id"
                android:inputType="text" />

            <!-- Vaccine type -->
            <EditText
                android:id="@+id/vac_txt_vaccine_type"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/vac_label_vaccine_type"
                android:inputType="text" />

            <!-- Site -->
            <EditText
                android:id="@+id/vac_txt_vaccine_site"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/vac_label_vaccine_site"
                android:inputType="text" />

            <!-- Date and time -->
            <EditText
                android:id="@+id/vac_txt_vaccine_datetime"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/vac_label_vaccine_datetime"
                android:inputType="datetime" />

        </LinearLayout>
    </ScrollView>

    <!-- List of vaccines -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Vacunas registradas:"
        android:textStyle="bold"
        android:padding="16dp"
        android:textSize="16sp" />

    <ListView
        android:id="@+id/vac_lst_vaccines"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp" />

</LinearLayout>